services:
  bot:
    build: .
    container_name: professor-thaddeus-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./main.py:/app/main.py:ro
      - ./thaddeus_bot:/app/thaddeus_bot:ro
      - ./state.json:/app/state.json
